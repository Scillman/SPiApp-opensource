#include maps\_anim;
#include maps\_utility;
#include common_scripts\utility;
#include maps\_utility_code;
#include maps\_fx;

main()
{
/*
	// Example Effect Setup.

	level._effect[ "fog_daytime" ]	   = loadfx( "weather/fog_daytime" ); // Loading the effect.
	// If you use weather/fog_daytime you must also load it in zone file with this line:	fx,weather/fog_daytime

	fog_origins = getentarray("fog_origins", "targetname"); // Put more than one script_origin in the map and give them targetname of fog_origins
	
	//The following will play the effect in all origins from above.
	for(i = 0; i < fog_origins.size; i++)
	{
	ent = maps\_utility::createLoopEffect( "fog_daytime" );
	ent.v[ "origin" ] = (fog_origins[i].origin);
	ent.v[ "angles" ] = (fog_origins[i].angles);
	}
*/
}